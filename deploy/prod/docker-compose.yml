version: "3.8"
services:
  mongo_insight:
    image: mongo
    ports:
      - "7675:27017"
    restart: always
    volumes:
      - mongodb:/DB/Mongo/data/db
    networks:
      - app_network

volumes:
  mongodb:

networks:
  app_network:
    external: true